import Replace from "../transformers/Replace";

interface Props {
  transformer: Replace
}

export const TransformerOptionsReplace = (props: Props) => {
  return (
      <div>This is replace</div>
  )
}